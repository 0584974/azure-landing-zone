name: Compare full vendor snapshot
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  compare:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Historical comparison workflow disabled during reconstruction."
