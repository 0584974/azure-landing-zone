# Networking

Describe the hub-spoke topology, address management, DNS ownership, and peering model.

## Sprint 1
This repository retains the existing networking modules and adds environment-specific networking configuration under `environments/<env>/networking.json`.
